import * as colors from './colors';
import * as fonts from './fonts';
import * as mediaTypes from './media';
import {ScreenEnum} from './screen-name';
import strings from './strings';

export {colors, fonts, mediaTypes, ScreenEnum, strings};
